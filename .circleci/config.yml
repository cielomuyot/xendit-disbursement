version: 2.1

orbs:
  heroku: circleci/heroku@1.2.6

jobs:
  build-and-test:
    docker:
      - image: node:14.17.6
    steps:
      - checkout
      - run:
          name: install-packages
          command: npm install
      - run:
          name: Run tests
          command: npm test
  build-test-deploy:
    docker:
      - image: node:14.17.6
    steps:
      - build-and-test
      - heroku/deploy-via-git

workflows:
  build-test:
    jobs:
      - build-and-test:
          filters:
            branches:
              ignore:
                - master
  build-test-deploy:
    jobs:
      - build-test-deploy:
          filters:
            branches:
              only:
                - master
